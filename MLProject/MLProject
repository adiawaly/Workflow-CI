name: food_delivery_training

conda_env: conda.yaml

entry_points:
  main:
    parameters:
      data_path: {type: str, default: "namadataset_preprocessing/food_delivery_cleaned.csv"}
    command: "python modelling.py --data_path {data_path}"